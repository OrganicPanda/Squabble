// Ensure namespace
var Squabble = Squabble || {};

// Squabble DOM Class. Used to interact with the DOM
Squabble.Input = function() {};
	
// Bind the given function to a user input
Squabble.Input.prototype.bind = function(action, handler) { 
	
} 

// Trigger a game action
Squabble.Input.prototype.trigger = function(action) { 
	
}
